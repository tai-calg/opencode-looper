# Milestone 5: ソース接続管理 + note.com 記事取込 + Embedding 自動生成

## 概要

/knowledge/sources でソース接続の追加・一覧表示ができ、/knowledge/sources/[id] で note.com API から記事一覧を取得・選択してナレッジとして取り込め、取込時に OpenAI API で embedding が自動生成される状態を実現する。source-management Context に Source/SourceArticle ドメインモデルと関連レイヤーを追加し、note.com 非公式 API を使った記事取得機能を実装する。

## タスク一覧

| ステップ | タスクID | Wave | 概要 |
|----------|----------|------|------|
| A | source-domain-contracts | 1 | Source/SourceArticle ドメインモデル + Repository/Gateway インターフェース + KnowledgeItem 拡張 + Unit テスト |
| B | source-infrastructure | 2 | PrismaSourceRepository + PrismaSourceArticleRepository + NoteApi/Stub ArticleFetchAdapter + PrismaKnowledgeRepository 拡張 |
| C | source-usecases | 2 | ListSources/CreateSource/GetSourceDetail/SyncArticles/ImportArticles UseCase + Unit テスト |
| D | source-presentation | 3 | Composition 更新 + Loader + Action |
| E | source-frontend | 4 | /knowledge/sources ページ + /knowledge/sources/[id] ページ + コンポーネント + ナレッジページ更新 |

## タスク詳細

---

### ステップ A: source-domain-contracts（Wave 1）

source-management コンテキストの domain 層に Source/SourceArticle ドメインモデル、Repository インターフェース、ArticleFetchGateway インターフェースを追加する。また、既存の KnowledgeItem モデルに sourceArticleId 対応を追加し、KnowledgeRepository に記事取込用のクエリメソッドを追加する。モデルの Unit テストも作成する。

#### 作成するファイル

1. `apps/webapp/src/backend/contexts/source-management/domain/models/source.model.ts`（新規）
2. `apps/webapp/src/backend/contexts/source-management/domain/models/source-article.model.ts`（新規）
3. `apps/webapp/src/backend/contexts/source-management/domain/repositories/source.repository.ts`（新規）
4. `apps/webapp/src/backend/contexts/source-management/domain/repositories/source-article.repository.ts`（新規）
5. `apps/webapp/src/backend/contexts/source-management/domain/gateways/article-fetch.gateway.ts`（新規）
6. `apps/webapp/src/backend/contexts/source-management/domain/models/knowledge-item.model.ts`（編集）
7. `apps/webapp/src/backend/contexts/source-management/domain/repositories/knowledge.repository.ts`（編集）
8. `apps/webapp/test/unit/contexts/source-management/domain/models/source.model.test.ts`（新規）
9. `apps/webapp/test/unit/contexts/source-management/domain/models/source-article.model.test.ts`（新規）

#### 型定義・インターフェース

```typescript
// source.model.ts
import { Result } from '@/backend/contexts/shared/domain/models/result.model';
import { Timestamp } from '@/backend/contexts/shared/domain/models/timestamp.model';

type SourceProps = {
	id: string;
	type: string;
	name: string;
	url: string;
	createdAt: Timestamp;
	updatedAt: Timestamp;
};

export class Source {
	private constructor(private readonly props: SourceProps) {}

	static create(params: {
		type: string;
		name: string;
		url: string;
	}): Result<Source, string> {
		// バリデーション:
		// - name.trim() が空なら Result.err('ソース名は必須です')
		// - url.trim() が空なら Result.err('URLは必須です')
		// - type !== 'note' なら Result.err('ソース種別は note のみサポートしています')
		// 成功時: id = crypto.randomUUID(), createdAt/updatedAt = Timestamp.now()
	}

	static reconstruct(props: SourceProps): Source {
		return new Source(props);
	}

	get id(): string { return this.props.id; }
	get type(): string { return this.props.type; }
	get name(): string { return this.props.name; }
	get url(): string { return this.props.url; }
	get createdAt(): Timestamp { return this.props.createdAt; }
	get updatedAt(): Timestamp { return this.props.updatedAt; }
}
```

```typescript
// source-article.model.ts
import { Result } from '@/backend/contexts/shared/domain/models/result.model';
import { Timestamp } from '@/backend/contexts/shared/domain/models/timestamp.model';

type SourceArticleProps = {
	id: string;
	sourceId: string;
	title: string;
	url: string;
	publishedAt: Timestamp | null;
	imported: boolean;
	createdAt: Timestamp;
};

export class SourceArticle {
	private constructor(private readonly props: SourceArticleProps) {}

	static create(params: {
		sourceId: string;
		title: string;
		url: string;
		publishedAt?: Date | null;
	}): Result<SourceArticle, string> {
		// バリデーション:
		// - title.trim() が空なら Result.err('タイトルは必須です')
		// - url.trim() が空なら Result.err('URLは必須です')
		// 成功時: id = crypto.randomUUID(), imported = false, createdAt = Timestamp.now()
		// publishedAt は params.publishedAt ? Timestamp.fromDate(params.publishedAt) : null
	}

	static reconstruct(props: SourceArticleProps): SourceArticle {
		return new SourceArticle(props);
	}

	markAsImported(): SourceArticle {
		return new SourceArticle({ ...this.props, imported: true });
	}

	get id(): string { return this.props.id; }
	get sourceId(): string { return this.props.sourceId; }
	get title(): string { return this.props.title; }
	get url(): string { return this.props.url; }
	get publishedAt(): Timestamp | null { return this.props.publishedAt; }
	get imported(): boolean { return this.props.imported; }
	get createdAt(): Timestamp { return this.props.createdAt; }
}
```

```typescript
// source.repository.ts
import type { Source } from '../models/source.model';

export interface SourceRepository {
	findAll(): Promise<Source[]>;
	findById(id: string): Promise<Source | null>;
	save(source: Source): Promise<void>;
	delete(id: string): Promise<void>;
}
```

```typescript
// source-article.repository.ts
import type { SourceArticle } from '../models/source-article.model';

export interface SourceArticleRepository {
	findBySourceId(sourceId: string): Promise<SourceArticle[]>;
	findByIds(ids: string[]): Promise<SourceArticle[]>;
	save(article: SourceArticle): Promise<void>;
	saveMany(articles: SourceArticle[]): Promise<void>;
	countBySourceId(sourceId: string): Promise<{ total: number; imported: number }>;
}
```

```typescript
// article-fetch.gateway.ts
export type ArticleListItem = {
	title: string;
	url: string;
	publishedAt: Date | null;
};

export interface ArticleFetchGateway {
	fetchArticleList(accountUrl: string): Promise<ArticleListItem[]>;
	fetchArticleContent(articleUrl: string): Promise<string>;
}
```

```typescript
// knowledge-item.model.ts（編集箇所のみ）
// create() の params に sourceArticleId を追加:
static create(params: {
	title: string;
	sourceType: string;
	content: string;
	sourceUrl?: string;
	sourceArticleId?: string;  // ← 追加
}): Result<KnowledgeItem, string> {
	// 既存のバリデーションはそのまま
	// 生成時: sourceArticleId: params.sourceArticleId ?? null
}
```

```typescript
// knowledge.repository.ts（編集箇所のみ）
// 既存のメソッドに加えて以下を追加:
export interface KnowledgeRepository {
	findAll(): Promise<KnowledgeItem[]>;
	findById(id: string): Promise<KnowledgeItem | null>;
	save(item: KnowledgeItem): Promise<void>;
	delete(id: string): Promise<void>;
	findBySourceArticleIds(sourceArticleIds: string[]): Promise<KnowledgeItem[]>;  // ← 追加
}
```

#### import 先（既存コード）

- `@/backend/contexts/shared/domain/models/result.model` → `Result`
- `@/backend/contexts/shared/domain/models/timestamp.model` → `Timestamp`

#### 実装パターン

- `ExpressionRule` モデル（`source-management/domain/models/expression-rule.model.ts`）と同じパターンで Source/SourceArticle を実装
- `KnowledgeItem` モデルのファクトリメソッド修正は、既存の `create()` メソッドの params に `sourceArticleId?: string` を追加し、生成時に `sourceArticleId: params.sourceArticleId ?? null` とするだけ
- KnowledgeRepository の `findBySourceArticleIds` は interface にメソッド追加のみ（実装はステップ B）

#### 注意事項

- Source.create() の type バリデーションは現時点では `'note'` のみ許可する
- SourceArticle.create() の publishedAt は Date → Timestamp 変換。null 許容
- KnowledgeItem.create() の既存テストに影響がないことを確認する（sourceArticleId はオプショナルなので既存テストは変更不要）

---

### ステップ B: source-infrastructure（Wave 2）

infrastructure 層に Source/SourceArticle の Prisma Repository 実装、note.com API アダプタ、Stub アダプタを作成する。また、既存の PrismaKnowledgeRepository に `findBySourceArticleIds` メソッドを追加する。

#### 作成するファイル

1. `apps/webapp/src/backend/contexts/source-management/infrastructure/repositories/prisma-source.repository.ts`（新規）
2. `apps/webapp/src/backend/contexts/source-management/infrastructure/repositories/prisma-source-article.repository.ts`（新規）
3. `apps/webapp/src/backend/contexts/source-management/infrastructure/repositories/prisma-knowledge.repository.ts`（編集）
4. `apps/webapp/src/backend/contexts/source-management/infrastructure/adapters/note-api-article-fetch.adapter.ts`（新規）
5. `apps/webapp/src/backend/contexts/source-management/infrastructure/adapters/stub-article-fetch.adapter.ts`（新規）

#### 型定義・インターフェース

```typescript
// prisma-source.repository.ts
import { Timestamp } from '@/backend/contexts/shared/domain/models/timestamp.model';
import { prisma } from '@/backend/contexts/shared/infrastructure/db/prisma-client';
import { Source } from '../../domain/models/source.model';
import type { SourceRepository } from '../../domain/repositories/source.repository';

export class PrismaSourceRepository implements SourceRepository {
	async findAll(): Promise<Source[]> {
		const rows = await prisma.source.findMany({ orderBy: { createdAt: 'desc' } });
		return rows.map((row) => this.toDomain(row));
	}

	async findById(id: string): Promise<Source | null> {
		const row = await prisma.source.findUnique({ where: { id } });
		return row ? this.toDomain(row) : null;
	}

	async save(source: Source): Promise<void> {
		await prisma.source.upsert({
			where: { id: source.id },
			create: {
				id: source.id,
				type: source.type,
				name: source.name,
				url: source.url,
				createdAt: source.createdAt.toDate(),
				updatedAt: source.updatedAt.toDate(),
			},
			update: {
				type: source.type,
				name: source.name,
				url: source.url,
				updatedAt: source.updatedAt.toDate(),
			},
		});
	}

	async delete(id: string): Promise<void> {
		await prisma.source.delete({ where: { id } });
	}

	private toDomain(row: {
		id: string;
		type: string;
		name: string;
		url: string;
		createdAt: Date;
		updatedAt: Date;
	}): Source {
		return Source.reconstruct({
			id: row.id,
			type: row.type,
			name: row.name,
			url: row.url,
			createdAt: Timestamp.fromDate(row.createdAt),
			updatedAt: Timestamp.fromDate(row.updatedAt),
		});
	}
}
```

```typescript
// prisma-source-article.repository.ts
import { Timestamp } from '@/backend/contexts/shared/domain/models/timestamp.model';
import { prisma } from '@/backend/contexts/shared/infrastructure/db/prisma-client';
import { SourceArticle } from '../../domain/models/source-article.model';
import type { SourceArticleRepository } from '../../domain/repositories/source-article.repository';

export class PrismaSourceArticleRepository implements SourceArticleRepository {
	async findBySourceId(sourceId: string): Promise<SourceArticle[]> {
		const rows = await prisma.sourceArticle.findMany({
			where: { sourceId },
			orderBy: { publishedAt: 'desc' },
		});
		return rows.map((row) => this.toDomain(row));
	}

	async findByIds(ids: string[]): Promise<SourceArticle[]> {
		const rows = await prisma.sourceArticle.findMany({ where: { id: { in: ids } } });
		return rows.map((row) => this.toDomain(row));
	}

	async save(article: SourceArticle): Promise<void> {
		await prisma.sourceArticle.upsert({
			where: { id: article.id },
			create: {
				id: article.id,
				sourceId: article.sourceId,
				title: article.title,
				url: article.url,
				publishedAt: article.publishedAt?.toDate() ?? null,
				imported: article.imported,
				createdAt: article.createdAt.toDate(),
			},
			update: {
				title: article.title,
				imported: article.imported,
			},
		});
	}

	async saveMany(articles: SourceArticle[]): Promise<void> {
		await prisma.sourceArticle.createMany({
			data: articles.map((a) => ({
				id: a.id,
				sourceId: a.sourceId,
				title: a.title,
				url: a.url,
				publishedAt: a.publishedAt?.toDate() ?? null,
				imported: a.imported,
				createdAt: a.createdAt.toDate(),
			})),
			skipDuplicates: true,
		});
	}

	async countBySourceId(sourceId: string): Promise<{ total: number; imported: number }> {
		const [total, imported] = await Promise.all([
			prisma.sourceArticle.count({ where: { sourceId } }),
			prisma.sourceArticle.count({ where: { sourceId, imported: true } }),
		]);
		return { total, imported };
	}

	private toDomain(row: {
		id: string;
		sourceId: string;
		title: string;
		url: string;
		publishedAt: Date | null;
		imported: boolean;
		createdAt: Date;
	}): SourceArticle {
		return SourceArticle.reconstruct({
			id: row.id,
			sourceId: row.sourceId,
			title: row.title,
			url: row.url,
			publishedAt: row.publishedAt ? Timestamp.fromDate(row.publishedAt) : null,
			imported: row.imported,
			createdAt: Timestamp.fromDate(row.createdAt),
		});
	}
}
```

```typescript
// prisma-knowledge.repository.ts（編集箇所のみ）
// 既存メソッドに加えて以下を追加:
async findBySourceArticleIds(sourceArticleIds: string[]): Promise<KnowledgeItem[]> {
	if (sourceArticleIds.length === 0) return [];
	const rows = await prisma.knowledgeItem.findMany({
		where: { sourceArticleId: { in: sourceArticleIds } },
	});
	return rows.map((row) => this.toDomain(row));
}
```

```typescript
// note-api-article-fetch.adapter.ts
import type { ArticleFetchGateway, ArticleListItem } from '../../domain/gateways/article-fetch.gateway';

export class NoteApiArticleFetchAdapter implements ArticleFetchGateway {
	async fetchArticleList(accountUrl: string): Promise<ArticleListItem[]> {
		// 1. URL からユーザー名を抽出: new URL(accountUrl).pathname.split('/').filter(Boolean)[0]
		// 2. ページネーションで全記事を取得:
		//    GET https://note.com/api/v2/creators/{username}/contents?kind=note&page={page}
		// 3. レスポンス形式:
		//    { data: { contents: [{ name, key, publishAt }], isLastPage: boolean } }
		// 4. 各記事を ArticleListItem に変換:
		//    title = item.name
		//    url = `https://note.com/${username}/n/${item.key}`
		//    publishedAt = item.publishAt ? new Date(item.publishAt) : null
		// 5. isLastPage === true または contents が空になるまでページングを繰り返す
	}

	async fetchArticleContent(articleUrl: string): Promise<string> {
		// 1. URL から noteKey を抽出: pathname の最後のセグメント
		// 2. GET https://note.com/api/v3/notes/{noteKey}
		// 3. レスポンス形式: { data: { body: string } } ← HTML 形式
		// 4. HTML タグを除去してプレーンテキストを返す:
		//    body.replace(/<[^>]*>/g, '').trim()
	}
}
```

```typescript
// stub-article-fetch.adapter.ts
import type { ArticleFetchGateway, ArticleListItem } from '../../domain/gateways/article-fetch.gateway';

export class StubArticleFetchAdapter implements ArticleFetchGateway {
	async fetchArticleList(_accountUrl: string): Promise<ArticleListItem[]> {
		return [
			{ title: 'テスト記事1', url: 'https://note.com/test/n/article1', publishedAt: new Date('2024-01-01') },
			{ title: 'テスト記事2', url: 'https://note.com/test/n/article2', publishedAt: new Date('2024-02-01') },
			{ title: 'テスト記事3', url: 'https://note.com/test/n/article3', publishedAt: new Date('2024-03-01') },
		];
	}

	async fetchArticleContent(_articleUrl: string): Promise<string> {
		return 'これはテスト用のスタブ記事本文です。実際の note.com API からは本物の記事本文が返されます。ナレッジとしてチェック対象との整合性確認に使用されます。';
	}
}
```

#### import 先（既存コード）

- `@/backend/contexts/shared/domain/models/timestamp.model` → `Timestamp`
- `@/backend/contexts/shared/infrastructure/db/prisma-client` → `prisma`
- `../../domain/models/source.model` → `Source`
- `../../domain/models/source-article.model` → `SourceArticle`
- `../../domain/models/knowledge-item.model` → `KnowledgeItem`
- `../../domain/repositories/source.repository` → `SourceRepository`
- `../../domain/repositories/source-article.repository` → `SourceArticleRepository`
- `../../domain/gateways/article-fetch.gateway` → `ArticleFetchGateway`, `ArticleListItem`

#### 実装パターン

- `PrismaExpressionRuleRepository`（`prisma-expression-rule.repository.ts`）と同じパターンで PrismaSourceRepository を実装
- `PrismaKnowledgeRepository`（`prisma-knowledge.repository.ts`）の既存パターンを踏襲して PrismaSourceArticleRepository を実装
- NoteApiArticleFetchAdapter は `fetch()` API を使った直接 HTTP リクエスト。外部ライブラリ不要

#### 注意事項

- note.com API はキー不要の非公式 API。レートリミットに注意し、ページング間に適度な待機を入れること
- NoteApiArticleFetchAdapter の `fetchArticleContent` は HTML → プレーンテキスト変換が必要。`/<[^>]*>/g` の正規表現で HTML タグを除去する
- PrismaSourceArticleRepository の `saveMany` は `createMany` + `skipDuplicates` で重複を無視する
- PrismaKnowledgeRepository の `findBySourceArticleIds` は既存の `toDomain` private メソッドをそのまま使える

---

### ステップ C: source-usecases（Wave 2）

application 層に Source 関連の 5 つの UseCase を作成し、Unit テストを追加する。

#### 作成するファイル

1. `apps/webapp/src/backend/contexts/source-management/application/usecases/list-sources.usecase.ts`（新規）
2. `apps/webapp/src/backend/contexts/source-management/application/usecases/create-source.usecase.ts`（新規）
3. `apps/webapp/src/backend/contexts/source-management/application/usecases/get-source-detail.usecase.ts`（新規）
4. `apps/webapp/src/backend/contexts/source-management/application/usecases/sync-articles.usecase.ts`（新規）
5. `apps/webapp/src/backend/contexts/source-management/application/usecases/import-articles.usecase.ts`（新規）
6. `apps/webapp/test/unit/contexts/source-management/application/usecases/source-usecases.test.ts`（新規）

#### 型定義・インターフェース

```typescript
// list-sources.usecase.ts
import type { SourceArticleRepository } from '../../domain/repositories/source-article.repository';
import type { SourceRepository } from '../../domain/repositories/source.repository';

export type SourceWithCounts = {
	source: Source;
	articleCount: number;
	importedCount: number;
};

export class ListSourcesUseCase {
	constructor(
		private readonly sourceRepository: SourceRepository,
		private readonly sourceArticleRepository: SourceArticleRepository,
	) {}

	async execute(): Promise<SourceWithCounts[]> {
		const sources = await this.sourceRepository.findAll();
		const result: SourceWithCounts[] = [];
		for (const source of sources) {
			const counts = await this.sourceArticleRepository.countBySourceId(source.id);
			result.push({
				source,
				articleCount: counts.total,
				importedCount: counts.imported,
			});
		}
		return result;
	}
}
```

```typescript
// create-source.usecase.ts
import { Source } from '../../domain/models/source.model';
import type { SourceRepository } from '../../domain/repositories/source.repository';

export class CreateSourceUseCase {
	constructor(private readonly sourceRepository: SourceRepository) {}

	async execute(params: { type: string; name: string; url: string }): Promise<Source> {
		const result = Source.create(params);
		if (!result.success) throw new Error(result.error);
		await this.sourceRepository.save(result.value);
		return result.value;
	}
}
```

```typescript
// get-source-detail.usecase.ts
import type { KnowledgeRepository } from '../../domain/repositories/knowledge.repository';
import type { SourceArticleRepository } from '../../domain/repositories/source-article.repository';
import type { SourceRepository } from '../../domain/repositories/source.repository';

export type SourceDetailResult = {
	source: Source;
	articles: SourceArticle[];
	knowledgeMapping: Map<string, string>; // sourceArticleId → knowledgeItemId
};

export class GetSourceDetailUseCase {
	constructor(
		private readonly sourceRepository: SourceRepository,
		private readonly sourceArticleRepository: SourceArticleRepository,
		private readonly knowledgeRepository: KnowledgeRepository,
	) {}

	async execute(sourceId: string): Promise<SourceDetailResult> {
		const source = await this.sourceRepository.findById(sourceId);
		if (!source) throw new Error('ソースが見つかりません');

		const articles = await this.sourceArticleRepository.findBySourceId(sourceId);
		const importedArticleIds = articles.filter((a) => a.imported).map((a) => a.id);

		let knowledgeMapping = new Map<string, string>();
		if (importedArticleIds.length > 0) {
			const knowledgeItems = await this.knowledgeRepository.findBySourceArticleIds(importedArticleIds);
			knowledgeMapping = new Map(
				knowledgeItems
					.filter((k) => k.sourceArticleId !== null)
					.map((k) => [k.sourceArticleId!, k.id]),
			);
		}

		return { source, articles, knowledgeMapping };
	}
}
```

```typescript
// sync-articles.usecase.ts
import type { ArticleFetchGateway } from '../../domain/gateways/article-fetch.gateway';
import { SourceArticle } from '../../domain/models/source-article.model';
import type { SourceArticleRepository } from '../../domain/repositories/source-article.repository';
import type { SourceRepository } from '../../domain/repositories/source.repository';

export class SyncArticlesUseCase {
	constructor(
		private readonly sourceRepository: SourceRepository,
		private readonly sourceArticleRepository: SourceArticleRepository,
		private readonly articleFetchGateway: ArticleFetchGateway,
	) {}

	async execute(sourceId: string): Promise<{ newCount: number }> {
		const source = await this.sourceRepository.findById(sourceId);
		if (!source) throw new Error('ソースが見つかりません');

		const fetched = await this.articleFetchGateway.fetchArticleList(source.url);
		const existing = await this.sourceArticleRepository.findBySourceId(sourceId);
		const existingUrls = new Set(existing.map((a) => a.url));

		const newArticles: SourceArticle[] = [];
		for (const item of fetched) {
			if (!existingUrls.has(item.url)) {
				const result = SourceArticle.create({
					sourceId,
					title: item.title,
					url: item.url,
					publishedAt: item.publishedAt,
				});
				if (result.success) newArticles.push(result.value);
			}
		}

		if (newArticles.length > 0) {
			await this.sourceArticleRepository.saveMany(newArticles);
		}
		return { newCount: newArticles.length };
	}
}
```

```typescript
// import-articles.usecase.ts
import type { EmbeddingGateway } from '@/backend/contexts/shared/domain/gateways/embedding.gateway';
import type { ArticleFetchGateway } from '../../domain/gateways/article-fetch.gateway';
import { KnowledgeItem } from '../../domain/models/knowledge-item.model';
import type { KnowledgeRepository } from '../../domain/repositories/knowledge.repository';
import type { SourceArticleRepository } from '../../domain/repositories/source-article.repository';

export class ImportArticlesUseCase {
	constructor(
		private readonly sourceArticleRepository: SourceArticleRepository,
		private readonly articleFetchGateway: ArticleFetchGateway,
		private readonly knowledgeRepository: KnowledgeRepository,
		private readonly embeddingGateway: EmbeddingGateway,
	) {}

	async execute(articleIds: string[]): Promise<{ importedCount: number }> {
		const articles = await this.sourceArticleRepository.findByIds(articleIds);
		let importedCount = 0;

		for (const article of articles) {
			if (article.imported) continue;

			// 1. 記事本文を note API から取得
			const content = await this.articleFetchGateway.fetchArticleContent(article.url);

			// 2. KnowledgeItem を作成（sourceArticleId を紐付け）
			const itemResult = KnowledgeItem.create({
				title: article.title,
				sourceType: 'note',
				content,
				sourceUrl: article.url,
				sourceArticleId: article.id,
			});
			if (!itemResult.success) continue;

			// 3. Embedding を生成
			const embedding = await this.embeddingGateway.generateEmbedding(content);
			const itemWithEmbedding = itemResult.value.withEmbedding(embedding);

			// 4. ナレッジを保存
			await this.knowledgeRepository.save(itemWithEmbedding);

			// 5. 記事を取込済みに更新
			const updatedArticle = article.markAsImported();
			await this.sourceArticleRepository.save(updatedArticle);

			importedCount++;
		}

		return { importedCount };
	}
}
```

#### import 先（既存コード）

- `@/backend/contexts/shared/domain/gateways/embedding.gateway` → `EmbeddingGateway`
- `../../domain/models/source.model` → `Source`
- `../../domain/models/source-article.model` → `SourceArticle`
- `../../domain/models/knowledge-item.model` → `KnowledgeItem`
- `../../domain/repositories/source.repository` → `SourceRepository`
- `../../domain/repositories/source-article.repository` → `SourceArticleRepository`
- `../../domain/repositories/knowledge.repository` → `KnowledgeRepository`
- `../../domain/gateways/article-fetch.gateway` → `ArticleFetchGateway`

#### 実装パターン

- `CreateKnowledgeUseCase`（`create-knowledge.usecase.ts`）と同じパターンで CreateSourceUseCase を実装
- `ListKnowledgeUseCase`（`list-knowledge.usecase.ts`）と同じパターンで ListSourcesUseCase を実装（カウント情報の付加あり）
- Unit テストは `knowledge-usecases.test.ts` と同じモック方式で実装

#### 注意事項

- `ImportArticlesUseCase` は逐次処理（for ループ）で1記事ずつ処理する。Promise.all による並列化は note API のレートリミット回避のためしない
- `SyncArticlesUseCase` は既存の URL と突合し、新規記事のみ追加する（既存記事の更新はしない）
- `GetSourceDetailUseCase` の knowledgeMapping は、取込済み記事のナレッジ詳細ページへのリンク生成に使用する

---

### ステップ D: source-presentation（Wave 3）

presentation 層の Composition Root に新しい UseCase ファクトリを追加し、Loader と Action を作成する。

#### 作成するファイル

1. `apps/webapp/src/backend/contexts/source-management/presentation/composition/source-management.composition.ts`（編集）
2. `apps/webapp/src/backend/contexts/source-management/presentation/loaders/source-list.loader.ts`（新規）
3. `apps/webapp/src/backend/contexts/source-management/presentation/loaders/source-detail.loader.ts`（新規）
4. `apps/webapp/src/backend/contexts/source-management/presentation/actions/create-source.action.ts`（新規）
5. `apps/webapp/src/backend/contexts/source-management/presentation/actions/sync-articles.action.ts`（新規）
6. `apps/webapp/src/backend/contexts/source-management/presentation/actions/import-articles.action.ts`（新規）

#### 型定義・インターフェース

```typescript
// source-management.composition.ts（追加分のみ）

// 新規 import:
import { CreateSourceUseCase } from '../../application/usecases/create-source.usecase';
import { GetSourceDetailUseCase } from '../../application/usecases/get-source-detail.usecase';
import { ImportArticlesUseCase } from '../../application/usecases/import-articles.usecase';
import { ListSourcesUseCase } from '../../application/usecases/list-sources.usecase';
import { SyncArticlesUseCase } from '../../application/usecases/sync-articles.usecase';
import type { ArticleFetchGateway } from '../../domain/gateways/article-fetch.gateway';
import { NoteApiArticleFetchAdapter } from '../../infrastructure/adapters/note-api-article-fetch.adapter';
import { PrismaSourceArticleRepository } from '../../infrastructure/repositories/prisma-source-article.repository';
import { PrismaSourceRepository } from '../../infrastructure/repositories/prisma-source.repository';

// 新規ファクトリ関数:
function createArticleFetchGateway(): ArticleFetchGateway {
	return new NoteApiArticleFetchAdapter();
}

export function createListSourcesUseCase(): ListSourcesUseCase {
	return new ListSourcesUseCase(
		new PrismaSourceRepository(),
		new PrismaSourceArticleRepository(),
	);
}

export function createCreateSourceUseCase(): CreateSourceUseCase {
	return new CreateSourceUseCase(new PrismaSourceRepository());
}

export function createGetSourceDetailUseCase(): GetSourceDetailUseCase {
	return new GetSourceDetailUseCase(
		new PrismaSourceRepository(),
		new PrismaSourceArticleRepository(),
		new PrismaKnowledgeRepository(),
	);
}

export function createSyncArticlesUseCase(): SyncArticlesUseCase {
	return new SyncArticlesUseCase(
		new PrismaSourceRepository(),
		new PrismaSourceArticleRepository(),
		createArticleFetchGateway(),
	);
}

export function createImportArticlesUseCase(): ImportArticlesUseCase {
	return new ImportArticlesUseCase(
		new PrismaSourceArticleRepository(),
		createArticleFetchGateway(),
		new PrismaKnowledgeRepository(),
		createEmbeddingGateway(),
	);
}
```

```typescript
// source-list.loader.ts
import { createListSourcesUseCase } from '../composition/source-management.composition';

export type SourceListItem = {
	id: string;
	type: string;
	name: string;
	url: string;
	articleCount: number;
	importedCount: number;
	createdAt: string; // ISO 8601
};

export async function loadSourceList(): Promise<SourceListItem[]> {
	const useCase = createListSourcesUseCase();
	const items = await useCase.execute();
	return items.map((item) => ({
		id: item.source.id,
		type: item.source.type,
		name: item.source.name,
		url: item.source.url,
		articleCount: item.articleCount,
		importedCount: item.importedCount,
		createdAt: item.source.createdAt.toISOString(),
	}));
}
```

```typescript
// source-detail.loader.ts
import { createGetSourceDetailUseCase } from '../composition/source-management.composition';

export type SourceDetailData = {
	source: {
		id: string;
		type: string;
		name: string;
		url: string;
		createdAt: string;
	};
	articles: {
		id: string;
		title: string;
		url: string;
		publishedAt: string | null;
		imported: boolean;
		knowledgeItemId: string | null;
	}[];
};

export async function loadSourceDetail(sourceId: string): Promise<SourceDetailData> {
	const useCase = createGetSourceDetailUseCase();
	const result = await useCase.execute(sourceId);
	return {
		source: {
			id: result.source.id,
			type: result.source.type,
			name: result.source.name,
			url: result.source.url,
			createdAt: result.source.createdAt.toISOString(),
		},
		articles: result.articles.map((a) => ({
			id: a.id,
			title: a.title,
			url: a.url,
			publishedAt: a.publishedAt?.toISOString() ?? null,
			imported: a.imported,
			knowledgeItemId: result.knowledgeMapping.get(a.id) ?? null,
		})),
	};
}
```

```typescript
// create-source.action.ts
'use server';

import { revalidatePath } from 'next/cache';
import { createCreateSourceUseCase } from '../composition/source-management.composition';

export async function createSource(params: {
	type: string;
	name: string;
	url: string;
}): Promise<{ success: boolean; error?: string }> {
	try {
		const useCase = createCreateSourceUseCase();
		await useCase.execute(params);
		revalidatePath('/knowledge/sources');
		return { success: true };
	} catch (error) {
		return {
			success: false,
			error: error instanceof Error ? error.message : '作成に失敗しました',
		};
	}
}
```

```typescript
// sync-articles.action.ts
'use server';

import { revalidatePath } from 'next/cache';
import { createSyncArticlesUseCase } from '../composition/source-management.composition';

export async function syncArticles(
	sourceId: string,
): Promise<{ success: boolean; newCount?: number; error?: string }> {
	try {
		const useCase = createSyncArticlesUseCase();
		const result = await useCase.execute(sourceId);
		revalidatePath(`/knowledge/sources/${sourceId}`);
		return { success: true, newCount: result.newCount };
	} catch (error) {
		return {
			success: false,
			error: error instanceof Error ? error.message : '同期に失敗しました',
		};
	}
}
```

```typescript
// import-articles.action.ts
'use server';

import { revalidatePath } from 'next/cache';
import { createImportArticlesUseCase } from '../composition/source-management.composition';

export async function importArticles(
	articleIds: string[],
	sourceId: string,
): Promise<{ success: boolean; importedCount?: number; error?: string }> {
	try {
		const useCase = createImportArticlesUseCase();
		const result = await useCase.execute(articleIds);
		revalidatePath(`/knowledge/sources/${sourceId}`);
		revalidatePath('/knowledge');
		return { success: true, importedCount: result.importedCount };
	} catch (error) {
		return {
			success: false,
			error: error instanceof Error ? error.message : '取り込みに失敗しました',
		};
	}
}
```

#### import 先（既存コード）

- `../composition/source-management.composition` → 各ファクトリ関数
- `next/cache` → `revalidatePath`

#### 実装パターン

- `create-knowledge.action.ts` と同じパターンで Action を実装（`'use server'` + try/catch + revalidatePath）
- `knowledge-list.loader.ts` と同じパターンで Loader を実装（UseCase 呼び出し → DTO 変換）
- Composition は既存の `source-management.composition.ts` に追記（既存の Rule/Knowledge ファクトリはそのまま維持）

#### 注意事項

- `import-articles.action.ts` は `/knowledge/sources/${sourceId}` と `/knowledge` の両方を revalidate する（ナレッジ一覧にも反映させるため）
- `createArticleFetchGateway()` は常に `NoteApiArticleFetchAdapter` を返す（note API はキー不要のため環境変数での切替不要）
- Loader の戻り値は全てプレーンオブジェクト（シリアライズ可能な DTO）にすること

---

### ステップ E: source-frontend（Wave 4）

/knowledge/sources ページと /knowledge/sources/[id] ページを作成し、SourceDialog コンポーネントと SourceArticleList コンポーネントを実装する。また、既存の /knowledge ページの「ソースから取り込む」ボタンを有効化する。

#### 作成するファイル

1. `apps/webapp/src/app/(auth)/knowledge/sources/page.tsx`（新規）
2. `apps/webapp/src/app/(auth)/knowledge/sources/[id]/page.tsx`（新規）
3. `apps/webapp/src/frontend/components/source-dialog.tsx`（新規）
4. `apps/webapp/src/frontend/components/source-article-list.tsx`（新規）
5. `apps/webapp/src/app/(auth)/knowledge/page.tsx`（編集）

#### 型定義・インターフェース

```typescript
// source-dialog.tsx
'use client';

// SourceDialog: 新規ソース追加ダイアログ
// Props:
type SourceDialogProps = {
	trigger: React.ReactNode;
	onSubmit: (params: {
		type: string;
		name: string;
		url: string;
	}) => Promise<{ success: boolean; error?: string }>;
};

// UI 構成:
// - Dialog (shadcn)
// - フィールド:
//   - ソース種別: Select (note のみ、将来拡張可能)
//   - アカウント名: Input (required)
//   - URL: Input (required, placeholder: "https://note.com/username")
// - 送信ボタン: "追加" / "追加中..."
// KnowledgeDialog と同じ Dialog パターンで実装
```

```typescript
// source-article-list.tsx
'use client';

// SourceArticleList: 記事一覧（チェックボックス選択 + 取込 + 同期）
// Props:
type SourceArticleListProps = {
	sourceId: string;
	articles: {
		id: string;
		title: string;
		url: string;
		publishedAt: string | null;
		imported: boolean;
		knowledgeItemId: string | null;
	}[];
	onSync: (sourceId: string) => Promise<{ success: boolean; newCount?: number; error?: string }>;
	onImport: (
		articleIds: string[],
		sourceId: string,
	) => Promise<{ success: boolean; importedCount?: number; error?: string }>;
};

// UI 構成:
// ヘッダー: 「記事一覧を再取得」ボタン (onSync 呼び出し)
//
// 未取込セクション:
//   - 「全選択 / 全解除」チェックボックス
//   - チェックボックス付き記事リスト（タイトル、公開日）
//   - 「選択した N 件を取り込む」ボタン (onImport 呼び出し、N = 選択数)
//   - 取込中は disabled + ローディング表示
//
// 取込済みセクション:
//   - 記事リスト（タイトル、公開日）
//   - ナレッジへのリンク: /knowledge/[knowledgeItemId] へ Link
//
// State:
//   - selectedIds: Set<string> — チェックされた記事ID
//   - syncing: boolean — 同期中フラグ
//   - importing: boolean — 取込中フラグ
```

```typescript
// knowledge/sources/page.tsx (Server Component)
import { createSource } from '@/backend/contexts/source-management/presentation/actions/create-source.action';
import {
	type SourceListItem,
	loadSourceList,
} from '@/backend/contexts/source-management/presentation/loaders/source-list.loader';
import { SourceDialog } from '@/frontend/components/source-dialog';
import { Button } from '@/frontend/components/ui/button';
import { Link2, Plus } from 'lucide-react';
import Link from 'next/link';

// UI 構成:
// ヘッダー: 「ソース接続」 + 「新規ソース追加」ボタン（SourceDialog のトリガー）
// ソースカード一覧:
//   各カード:
//   - ソース種別アイコン (Link2)、名前、URL
//   - 取込済み N 件 / 全 M 件
//   - カードクリックで /knowledge/sources/{id} へ遷移 (Link)
// 空状態: 「ソース接続はまだありません」
```

```typescript
// knowledge/sources/[id]/page.tsx (Server Component)
import { importArticles } from '@/backend/contexts/source-management/presentation/actions/import-articles.action';
import { syncArticles } from '@/backend/contexts/source-management/presentation/actions/sync-articles.action';
import {
	type SourceDetailData,
	loadSourceDetail,
} from '@/backend/contexts/source-management/presentation/loaders/source-detail.loader';
import { SourceArticleList } from '@/frontend/components/source-article-list';
import { ArrowLeft } from 'lucide-react';
import Link from 'next/link';

// UI 構成:
// ヘッダー:
//   - 「← ソース一覧」戻るリンク (/knowledge/sources)
//   - ソース名 + URL (外部リンク)
//   - ソース種別バッジ
// SourceArticleList コンポーネント（articles, onSync, onImport を渡す）
```

```typescript
// knowledge/page.tsx（編集箇所のみ）
// 「ソースから取り込む」ボタンを disabled から Link に変更:

// Before:
// <Button variant="outline" disabled>ソースから取り込む</Button>

// After:
import Link from 'next/link';
// ...
<Button variant="outline" asChild>
	<Link href="/knowledge/sources">ソースから取り込む</Link>
</Button>
```

#### import 先（既存コード）

- `@/backend/contexts/source-management/presentation/actions/create-source.action` → `createSource`
- `@/backend/contexts/source-management/presentation/actions/sync-articles.action` → `syncArticles`
- `@/backend/contexts/source-management/presentation/actions/import-articles.action` → `importArticles`
- `@/backend/contexts/source-management/presentation/loaders/source-list.loader` → `loadSourceList`, `SourceListItem`
- `@/backend/contexts/source-management/presentation/loaders/source-detail.loader` → `loadSourceDetail`, `SourceDetailData`
- `@/frontend/components/source-dialog` → `SourceDialog`
- `@/frontend/components/source-article-list` → `SourceArticleList`
- `@/frontend/components/ui/button` → `Button`
- `@/frontend/components/ui/dialog` → `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogTrigger`
- `@/frontend/components/ui/input` → `Input`
- `@/frontend/components/ui/label` → `Label`
- `@/frontend/components/ui/select` → `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue`
- `@/frontend/components/ui/checkbox` → `Checkbox`（shadcn/ui。未インストールの場合は `npx shadcn@latest add checkbox` で追加）
- `lucide-react` → `Link2`, `Plus`, `ArrowLeft`, `RefreshCw`, `Download`, `ExternalLink`
- `next/link` → `Link`

#### 実装パターン

- `SourceDialog` は `KnowledgeDialog`（`knowledge-dialog.tsx`）と同じ Dialog パターンで実装
- `SourceArticleList` は `'use client'` の Client Component。useState で選択状態を管理
- ページ（`page.tsx`）は Server Component。loader でデータ取得 → コンポーネントに props 渡し
- `/knowledge/page.tsx` の編集は Button の `disabled` 属性を削除し、`asChild` + `Link` に置き換えるだけ

#### 注意事項

- `Checkbox` コンポーネントが shadcn/ui にまだ追加されていない場合は `npx shadcn@latest add checkbox` を実行すること
- SourceArticleList の取込処理は時間がかかる可能性がある（記事数 × API 呼び出し）。importing 状態でボタンを disabled にし、ローディング表示を出すこと
- 取込済み記事のナレッジリンクは `knowledgeItemId` が null でないもののみ表示
- 記事の公開日は `publishedAt` が null の場合は「日付不明」と表示
