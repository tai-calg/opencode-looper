# Milestone 6: Slack ボットメンションによる自動チェック + スレッド投稿

## 概要

Slack でボットにメンションすると、そのスレッドの全メッセージを取得し Claude API で5観点の自動チェックを実行。完了後、指摘件数サマリーと Web チェック結果ページへのパーマリンクを Slack スレッドに投稿する。

## タスク一覧

| ステップ | タスク ID | Wave | 概要 |
|---------|----------|------|------|
| A | slack-domain-contracts | 1 | SlackGateway インターフェース + Check/RunCheckUseCase のパラメータ拡張 |
| B | slack-infrastructure | 2 | SlackWebApiAdapter + StubSlackAdapter 実装 |
| C | slack-usecase | 2 | HandleSlackMentionUseCase + Unit テスト |
| D | slack-presentation | 3 | Composition 更新 + API Route |

## タスク詳細

### ステップ A: slack-domain-contracts（Wave 1）

SlackGateway インターフェースを定義し、既存の Check モデルと RunCheckUseCase のパラメータを拡張して Slack 由来のチェックに対応する。

#### 作成するファイル

- `apps/webapp/src/backend/contexts/content-check/domain/gateways/slack.gateway.ts`（新規）

#### 編集するファイル

- `apps/webapp/src/backend/contexts/content-check/domain/models/check.model.ts`（編集）
- `apps/webapp/src/backend/contexts/content-check/application/usecases/run-check.usecase.ts`（編集）
- `apps/webapp/src/backend/contexts/content-check/presentation/actions/create-check.action.ts`（編集）
- `apps/webapp/test/unit/contexts/content-check/domain/models/check.model.test.ts`（編集）
- `apps/webapp/test/unit/contexts/content-check/application/usecases/run-check.usecase.test.ts`（編集）

#### 型定義・インターフェース

```typescript
// slack.gateway.ts
export interface SlackGateway {
  /**
   * スレッドの全メッセージテキストを取得する。
   * Slack API conversations.replies を使用。
   */
  fetchThreadMessages(channel: string, threadTs: string): Promise<string[]>;

  /**
   * チャンネル（またはスレッド）にメッセージを投稿する。
   * Slack API chat.postMessage を使用。
   */
  postMessage(channel: string, text: string, threadTs?: string): Promise<void>;
}
```

```typescript
// check.model.ts — Check.create() のパラメータを拡張
// 既存の create メソッドのパラメータに source, slackChannel, slackThreadTs を追加

static create(params: {
  title?: string;
  platform?: string;
  content: string;
  source: string;          // 追加: 'web' | 'slack'
  slackChannel?: string;   // 追加: Slack チャンネル ID
  slackThreadTs?: string;  // 追加: Slack スレッド timestamp
  userId?: string;
}): Result<Check, string>

// create 内部で slackChannel, slackThreadTs を props に設定する
// 既存の source: 'web' ハードコードを削除し、params.source を使用する
```

```typescript
// run-check.usecase.ts — execute() のパラメータを拡張

async execute(params: {
  title?: string;
  platform?: string;
  content: string;
  source?: string;          // 追加: デフォルト 'web'
  slackChannel?: string;    // 追加
  slackThreadTs?: string;   // 追加
  userId?: string;
}): Promise<string>

// Check.create() 呼び出し時に source, slackChannel, slackThreadTs を渡す
```

```typescript
// create-check.action.ts — 明示的に source: 'web' を渡すように変更
const checkId = await useCase.execute({
  ...params,
  source: 'web',  // 追加: 明示的に Web 由来であることを指定
});
```

#### import 先（既存コード）

- `@/backend/contexts/shared/domain/models/result.model` → `Result`
- `@/backend/contexts/shared/domain/models/timestamp.model` → `Timestamp`

#### 実装パターン

- SlackGateway インターフェースは `ExpressionRuleQueryGateway`（`content-check/domain/gateways/expression-rule-query.gateway.ts`）と同じパターンで定義
- Check.create() の拡張は既存のファクトリメソッドに params を追加するだけ。`slackChannel` と `slackThreadTs` は既に CheckProps に定義済み（現在は `null` 固定）
- RunCheckUseCase.execute() は params をそのまま Check.create() に透過する

#### 注意事項

- Check モデルの `source` フィールドは既に props に存在し DB スキーマにも定義済み。現在は `Check.create()` 内で `'web'` にハードコードされている。これを params から受け取るように変更する
- `slackChannel` と `slackThreadTs` も同様に CheckProps と DB スキーマに既に存在。`null` 初期化されている部分を params から設定するように変更する
- **DB スキーマ（Prisma）の変更は不要**。全フィールドが既に定義済み
- 既存の Unit テストで `Check.create()` を呼んでいる箇所に `source: 'web'` を追加する
- RunCheckUseCase のテストも同様に `source` パラメータを追加する

---

### ステップ B: slack-infrastructure（Wave 2）

Slack Web API を使ったアダプタ実装（本番 + Stub）を作成する。外部パッケージは追加せず、`fetch` で直接 Slack API を呼び出す。

#### 作成するファイル

- `apps/webapp/src/backend/contexts/content-check/infrastructure/adapters/slack-web-api.adapter.ts`（新規）
- `apps/webapp/src/backend/contexts/content-check/infrastructure/adapters/stub-slack.adapter.ts`（新規）

#### 型定義・インターフェース

```typescript
// slack-web-api.adapter.ts
import type { SlackGateway } from '../../domain/gateways/slack.gateway';

export class SlackWebApiAdapter implements SlackGateway {
  constructor(private readonly botToken: string) {}

  async fetchThreadMessages(channel: string, threadTs: string): Promise<string[]> {
    // GET https://slack.com/api/conversations.replies?channel={channel}&ts={threadTs}
    // Authorization: Bearer {botToken}
    // レスポンスの messages[].text を返す
    // ボットメッセージ（subtype: 'bot_message' または bot_id 付き）は除外する
    // メンションパターン <@U...> をテキストから除去する
  }

  async postMessage(channel: string, text: string, threadTs?: string): Promise<void> {
    // POST https://slack.com/api/chat.postMessage
    // Authorization: Bearer {botToken}
    // Content-Type: application/json
    // body: { channel, text, thread_ts: threadTs }
  }
}
```

```typescript
// stub-slack.adapter.ts
import type { SlackGateway } from '../../domain/gateways/slack.gateway';

export class StubSlackAdapter implements SlackGateway {
  async fetchThreadMessages(_channel: string, _threadTs: string): Promise<string[]> {
    // 固定のテストメッセージを返す
    return [
      'これはテスト用のスレッドメッセージです。',
      '二つ目のメッセージです。内容をチェックしてください。',
    ];
  }

  async postMessage(channel: string, text: string, threadTs?: string): Promise<void> {
    // コンソールにログ出力のみ
    console.log('[StubSlack] postMessage:', { channel, text: text.slice(0, 100), threadTs });
  }
}
```

#### import 先（既存コード）

- `@/backend/contexts/content-check/domain/gateways/slack.gateway` → `SlackGateway`

#### 実装パターン

- `AnthropicAIAdapter`（`shared/infrastructure/adapters/anthropic-ai.adapter.ts`）と同じパターン
  - コンストラクタでトークン等を受け取る
  - 外部 API 呼び出しは `fetch` で実装
- `StubAIAdapter`（`shared/infrastructure/adapters/stub-ai.adapter.ts`）と同じパターン
  - 固定値を返し、コンソールにログ出力

#### 注意事項

- **外部パッケージ（`@slack/web-api` 等）は追加しない**。Slack Web API は REST なので `fetch` で十分
- Slack API レスポンスの `ok` フィールドが `false` の場合はエラーを throw する
- `conversations.replies` のレスポンスでボットのメッセージは除外する。`bot_id` フィールドまたは `subtype === 'bot_message'` で判定
- メンションパターン `<@U[A-Z0-9]+>` をテキストから正規表現で除去する（チェック対象に含めないため）
- Slack API の URL:
  - `https://slack.com/api/conversations.replies`
  - `https://slack.com/api/chat.postMessage`

---

### ステップ C: slack-usecase（Wave 2）

Slack メンション受信時の全体フローを orchestrate する HandleSlackMentionUseCase を作成する。

#### 作成するファイル

- `apps/webapp/src/backend/contexts/content-check/application/usecases/handle-slack-mention.usecase.ts`（新規）
- `apps/webapp/test/unit/contexts/content-check/application/usecases/handle-slack-mention.usecase.test.ts`（新規）

#### 型定義・インターフェース

```typescript
// handle-slack-mention.usecase.ts
import type { SlackGateway } from '../../domain/gateways/slack.gateway';
import type { CheckRepository } from '../../domain/repositories/check.repository';
import type { Check } from '../../domain/models/check.model';
import type { RunCheckUseCase } from './run-check.usecase';

export class HandleSlackMentionUseCase {
  constructor(
    private readonly slackGateway: SlackGateway,
    private readonly runCheckUseCase: RunCheckUseCase,
    private readonly checkRepository: CheckRepository,
  ) {}

  async execute(params: {
    channel: string;
    threadTs: string;
    appUrl: string;
  }): Promise<void> {
    // 1. スレッドの全メッセージを取得
    const messages = await this.slackGateway.fetchThreadMessages(
      params.channel,
      params.threadTs,
    );

    // 2. メッセージを結合してチェック対象テキストを生成
    const content = messages.join('\n\n');

    // 3. テキストが空の場合は早期リターン（メッセージ投稿のみ）
    if (content.trim().length === 0) {
      await this.slackGateway.postMessage(
        params.channel,
        'スレッドにチェック対象のメッセージが見つかりませんでした。',
        params.threadTs,
      );
      return;
    }

    // 4. RunCheckUseCase でチェック実行（同期的に完了まで待機）
    const checkId = await this.runCheckUseCase.execute({
      content,
      source: 'slack',
      slackChannel: params.channel,
      slackThreadTs: params.threadTs,
    });

    // 5. チェック結果を取得
    const check = await this.checkRepository.findById(checkId);
    if (!check) {
      await this.slackGateway.postMessage(
        params.channel,
        'チェック結果の取得に失敗しました。',
        params.threadTs,
      );
      return;
    }

    // 6. サマリーメッセージをフォーマットして投稿
    const summary = this.formatSummary(check, params.appUrl, checkId);
    await this.slackGateway.postMessage(
      params.channel,
      summary,
      params.threadTs,
    );
  }

  private formatSummary(check: Check, appUrl: string, checkId: string): string {
    // チェック結果からカテゴリ別・重要度別の件数を集計
    const issues = check.sections.flatMap((s) => s.issues);
    const totalCount = issues.length;

    if (totalCount === 0) {
      return [
        'チェックが完了しました。指摘事項はありません。',
        '',
        `詳細を確認: ${appUrl}/checks/${checkId}`,
      ].join('\n');
    }

    // カテゴリ別集計
    const byCategory = {
      fact: issues.filter((i) => i.category === 'fact').length,
      knowledge: issues.filter((i) => i.category === 'knowledge').length,
      expression: issues.filter((i) => i.category === 'expression').length,
      risk: issues.filter((i) => i.category === 'risk').length,
      quality: issues.filter((i) => i.category === 'quality').length,
    };

    // 重要度別集計
    const needsFix = issues.filter((i) => i.severity === 'needs_fix').length;
    const caution = issues.filter((i) => i.severity === 'caution').length;

    return [
      `チェックが完了しました。指摘: ${totalCount}件`,
      '',
      `  事実確認: ${byCategory.fact}件`,
      `  ナレッジ整合: ${byCategory.knowledge}件`,
      `  表現ルール: ${byCategory.expression}件`,
      `  炎上リスク: ${byCategory.risk}件`,
      `  文章品質: ${byCategory.quality}件`,
      '',
      '重要度:',
      `  要修正: ${needsFix}件`,
      `  注意: ${caution}件`,
      '',
      `詳細を確認: ${appUrl}/checks/${checkId}`,
    ].join('\n');
  }
}
```

#### import 先（既存コード）

- `@/backend/contexts/content-check/domain/gateways/slack.gateway` → `SlackGateway`
- `@/backend/contexts/content-check/domain/repositories/check.repository` → `CheckRepository`
- `@/backend/contexts/content-check/domain/models/check.model` → `Check`
- `@/backend/contexts/content-check/application/usecases/run-check.usecase` → `RunCheckUseCase`
- `@/backend/contexts/content-check/domain/models/check-category.model` → `CheckCategory`（カテゴリ文字列の型参照に使用）
- `@/backend/contexts/content-check/domain/models/severity.model` → `Severity`（重要度文字列の型参照に使用）

#### 実装パターン

- `RunCheckUseCase`（`content-check/application/usecases/run-check.usecase.ts`）と同じパターン
  - クラスベース + コンストラクタ DI
  - `execute()` メソッドでフロー実行
  - private ヘルパーメソッドで詳細処理
- RunCheckUseCase は既に構成済みのインスタンスをコンストラクタで受け取る（再構成しない）

#### Unit テスト設計

```typescript
// handle-slack-mention.usecase.test.ts
describe('HandleSlackMentionUseCase', () => {
  // モック
  // - SlackGateway: fetchThreadMessages → テストメッセージ返却, postMessage → 呼び出し記録
  // - RunCheckUseCase: execute → 固定 checkId 返却
  // - CheckRepository: findById → テスト用 Check オブジェクト返却

  it('スレッドメッセージを取得してチェックを実行し、結果サマリーを投稿する', ...);
  it('チェック結果に指摘がない場合、「指摘事項はありません」メッセージを投稿する', ...);
  it('スレッドにメッセージがない場合、エラーメッセージを投稿する', ...);
  it('RunCheckUseCase に source: slack, slackChannel, slackThreadTs を渡す', ...);
  it('サマリーメッセージにカテゴリ別・重要度別の件数とパーマリンクが含まれる', ...);
});
```

#### 注意事項

- RunCheckUseCase.execute() は同期的に（await で）チェック完了まで待機する。非同期ストリームではない
- formatSummary のテキストフォーマットはプレーンテキスト。Slack Block Kit は使用しない（将来の拡張ポイント）
- Check の `sections` → `issues` のネストアクセスが必要。Check モデルの getter を使用する
- issues の `category` と `severity` は文字列型（enum ではない）。型ガードではなく文字列比較で集計する

---

### ステップ D: slack-presentation（Wave 3）

Composition に Slack 関連の DI を追加し、Slack Events API の Webhook 受信 API Route を作成する。

#### 編集するファイル

- `apps/webapp/src/backend/contexts/content-check/presentation/composition/content-check.composition.ts`（編集）

#### 作成するファイル

- `apps/webapp/src/app/api/slack/events/route.ts`（新規）

#### 型定義・インターフェース

```typescript
// content-check.composition.ts — 追加部分

import type { SlackGateway } from '../../domain/gateways/slack.gateway';
import { SlackWebApiAdapter } from '../../infrastructure/adapters/slack-web-api.adapter';
import { StubSlackAdapter } from '../../infrastructure/adapters/stub-slack.adapter';
import { HandleSlackMentionUseCase } from '../../application/usecases/handle-slack-mention.usecase';

// 既存のファクトリ関数群の後に追加:

function createSlackGateway(): SlackGateway {
  return process.env.SLACK_BOT_TOKEN
    ? new SlackWebApiAdapter(process.env.SLACK_BOT_TOKEN)
    : new StubSlackAdapter();
}

export function createHandleSlackMentionUseCase(): HandleSlackMentionUseCase {
  return new HandleSlackMentionUseCase(
    createSlackGateway(),
    createRunCheckUseCase(),
    new PrismaCheckRepository(),
  );
}
```

```typescript
// app/api/slack/events/route.ts
import { after } from 'next/server';
import { createHmac, timingSafeEqual } from 'node:crypto';
import { createHandleSlackMentionUseCase } from '@/backend/contexts/content-check/presentation/composition/content-check.composition';

// Vercel Fluid Compute: AI チェック処理に十分な時間を確保
export const maxDuration = 300;

export async function POST(request: Request): Promise<Response> {
  // 1. Slack リトライの場合はスキップ（重複処理防止）
  if (request.headers.get('x-slack-retry-num')) {
    return Response.json({ ok: true });
  }

  // 2. リクエストボディを raw text で取得（署名検証に必要）
  const rawBody = await request.text();

  // 3. 署名検証
  const timestamp = request.headers.get('x-slack-request-timestamp') ?? '';
  const signature = request.headers.get('x-slack-signature') ?? '';
  const signingSecret = process.env.SLACK_SIGNING_SECRET ?? '';

  if (!verifySlackSignature(rawBody, timestamp, signature, signingSecret)) {
    return new Response('Invalid signature', { status: 401 });
  }

  // 4. ペイロード解析
  const payload = JSON.parse(rawBody) as SlackEventPayload;

  // 5. URL verification challenge（Slack App 初回設定時）
  if (payload.type === 'url_verification') {
    return Response.json({ challenge: payload.challenge });
  }

  // 6. app_mention イベント処理
  if (payload.type === 'event_callback' && payload.event?.type === 'app_mention') {
    const { channel, thread_ts, ts } = payload.event;
    const threadTs = thread_ts ?? ts; // スレッド外メンションの場合はメッセージ自身の ts を使用
    const appUrl = new URL(request.url).origin;

    after(async () => {
      try {
        const useCase = createHandleSlackMentionUseCase();
        await useCase.execute({ channel, threadTs, appUrl });
      } catch (error) {
        console.error('[Slack] HandleSlackMention failed:', error);
      }
    });
  }

  // 7. Slack に即座に 200 応答（3秒以内のレスポンス要件を満たす）
  return Response.json({ ok: true });
}

// --- Slack 署名検証 ---

function verifySlackSignature(
  body: string,
  timestamp: string,
  signature: string,
  secret: string,
): boolean {
  if (!timestamp || !signature || !secret) {
    return false;
  }

  // リプレイ攻撃防止: 5分以上前のリクエストは拒否
  const fiveMinutesAgo = Math.floor(Date.now() / 1000) - 300;
  if (Number.parseInt(timestamp, 10) < fiveMinutesAgo) {
    return false;
  }

  const sigBaseString = `v0:${timestamp}:${body}`;
  const mySignature = `v0=${createHmac('sha256', secret).update(sigBaseString).digest('hex')}`;

  try {
    return timingSafeEqual(Buffer.from(mySignature), Buffer.from(signature));
  } catch {
    return false;
  }
}

// --- Slack Event 型定義（ファイル内ローカル） ---

type SlackEventPayload = {
  type: string;
  challenge?: string;
  event?: {
    type: string;
    user: string;
    text: string;
    ts: string;
    channel: string;
    thread_ts?: string;
  };
};
```

#### import 先（既存コード）

**composition.ts の追加 import:**
- `@/backend/contexts/content-check/domain/gateways/slack.gateway` → `SlackGateway`
- `@/backend/contexts/content-check/infrastructure/adapters/slack-web-api.adapter` → `SlackWebApiAdapter`
- `@/backend/contexts/content-check/infrastructure/adapters/stub-slack.adapter` → `StubSlackAdapter`
- `@/backend/contexts/content-check/application/usecases/handle-slack-mention.usecase` → `HandleSlackMentionUseCase`

**route.ts の import:**
- `next/server` → `after`
- `node:crypto` → `createHmac`, `timingSafeEqual`
- `@/backend/contexts/content-check/presentation/composition/content-check.composition` → `createHandleSlackMentionUseCase`

#### 実装パターン

- Composition のファクトリ関数は既存の `createRunCheckUseCase()` と同じパターン。環境変数で本番/Stub を切り替え
- API Route は `app/api/checks/[id]/route.ts` と同じパターン（Route Handler）。ただし POST のみ
- `after()` は Next.js 15.1+ の安定 API。Vercel デプロイ時は自動的に `waitUntil` にマッピングされ、Fluid Compute で `maxDuration` まで実行が延長される

#### 注意事項

- **`after()` は `next/server` から import する**。`@vercel/functions` の `waitUntil` ではない。Next.js 15.1+ で安定化済みで、Vercel 上では自動的に `waitUntil` として動作する
- **`maxDuration = 300`**（5分）を export する。AI チェック処理には30秒〜数分かかるため、デフォルトの10秒では不足
- **署名検証は raw body で行う**。`request.text()` で生のリクエストボディを取得し、署名検証後に `JSON.parse()` する
- **Slack リトライヘッダー `x-slack-retry-num`** が存在する場合は即座に 200 を返す（重複チェック防止）
- **リプレイ攻撃防止**: タイムスタンプが5分以上前のリクエストは拒否する
- **`thread_ts` がない場合**（スレッド外メンション）: メッセージ自身の `ts` を `threadTs` として使用。スレッド内の他メッセージは取得できないが、メンションメッセージの内容のみチェックする
- **appUrl はリクエスト URL から導出**: `new URL(request.url).origin` で取得。Vercel デプロイ時はデプロイ先の URL、ローカルでは `http://localhost:3000` になる
- **エラーハンドリング**: `after()` 内の例外は `console.error` でログ出力。Slack には既に 200 を返しているため、エラー通知は Vercel ログで確認する
- **Slack Event 型は route.ts 内にローカル定義する**。外部パッケージの型は使用しない
- API Route は外部 Webhook 受信の例外ケースとしてアーキテクチャ上許可されている（`docs/architecture.md` の「API Route は外部 Webhook 受信等の例外のみ許可」に該当）
