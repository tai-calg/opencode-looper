# Slack 連携契約定義

## タスク ID
slack-contracts

## 内容
Slack 連携に必要な契約定義を実装。

## 作成・変更したファイル
- **新規作成**: `apps/content-reviewer/src/backend/contexts/content-check/domain/gateways/slack.gateway.ts`
  - `SlackGateway` interface: `postMessage(channelId, threadTs, text): Promise<void>`
- **変更**: `apps/content-reviewer/src/backend/contexts/content-check/domain/models/content-check.model.ts`
  - `ContentCheckProps` に `slackChannelId: string | null` と `slackThreadTs: string | null` を追加
  - `ContentCheck` クラスのフィールド・コンストラクタ・`create()` props・`toProps()` を更新
- **変更**: `apps/content-reviewer/src/backend/contexts/content-check/infrastructure/repositories/prisma-content-check.repository.ts`
  - `toDomain()` と `toPrisma()` で slack フィールドをマッピング
- **変更（テスト）**: `reconstruct` 呼び出し箇所に `slackChannelId: null, slackThreadTs: null` を追加
  - `test/unit/.../content-check.model.test.ts`
  - `test/unit/.../list-content-checks.usecase.test.ts`
  - `src/.../get-content-check-detail.usecase.test.ts`
  - `test/unit/.../prisma-content-check.repository.test.ts`

## 設計判断
- `ContentCheck.create()` の `slackChannelId`/`slackThreadTs` は省略可能（`?`）にして後方互換を維持
- `ContentCheckProps`（reconstruct 用）は必須フィールドとして型安全に定義
- `SlackGateway` は domain/gateways に配置（DDD 4 層の依存ルール遵守）

## 次のタスクへの申し送り
- W2 以降のタスクが `SlackGateway` と `slackChannelId`/`slackThreadTs` を参照する
- Prisma スキーマの `slackChannelId`/`slackThreadTs` カラムは既に定義済み（既存コード確認済）
- `execute-content-check.usecase.ts` への Slack 通知連携は W2 以降で実装
