# タスク: check-history-table-component

## 内容
`src/components/dashboard/check-history-table.tsx` を新規作成（Server Component）

## 作成・変更ファイル
- `apps/content-reviewer/src/components/dashboard/check-history-table.tsx`（新規作成）

## 設計判断
- Server Component として実装（`'use client'` 不要。インタラクションなし）
- `ContentCheckListItemDto[]` を props で受け取る
- shadcn/ui の `Table` / `Badge` を使って一覧表示
- カラム構成: ソース種別（Web/Slack バッジ）・ステータス（pending/processing/completed/failed をカラーバッジ）・サマリー（error/warning/info 件数）・実行日時（日本語フォーマット）・操作（詳細へリンク）
- 各セルを `<Link href="/checks/[id]">` で包み行全体をリンク化
- データ0件時は「チェック履歴がありません」を表示
- Biome フォーマット規約（タブ・シングルクォート・セミコロン）に従い、長い Badge は1行で記述

## 詰まった点・解決方法
- Biome が `<Badge>失敗</Badge>` を1行に整形する規約あり。複数行で書くとフォーマットエラー → 1行に修正

## 次のタスクへの申し送り
- ダッシュボードページ（`/` の page.tsx）からこのコンポーネントを使う際は loader で `ContentCheckListItemDto[]` を取得して渡す必要がある
- `components/dashboard/` ディレクトリは今回初めて作成
