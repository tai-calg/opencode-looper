# タスク: check-history-filter-component

## 内容
`src/components/dashboard/check-history-filter.tsx` を新規作成（'use client'）。
ソース種別・ステータス・実行日時範囲のフィルタ UI コンポーネント。

## 作成・変更したファイル
- `apps/content-reviewer/package.json` — `@radix-ui/react-select` 依存を追加
- `apps/content-reviewer/src/components/ui/select.tsx` — shadcn/ui 標準 Select コンポーネントを新規作成
- `apps/content-reviewer/src/components/dashboard/check-history-filter.tsx` — フィルタ UI コンポーネントを新規作成

## 設計判断
- `@radix-ui/react-select` が未インストールだったため package.json に追加し pnpm install を実行
- Select コンポーネントは shadcn/ui の標準実装パターンに従い `components/ui/select.tsx` に作成
- フィルタ変更時は `useSearchParams` で現在のパラメータを読み取り、変更のみ上書きして `router.push` で URL 更新
- 「全て」選択時は対応する searchParam を削除してクリーンな URL を保つ
- Biome フォーマットエラー（`<Select>` タグのプロパティ展開）は `pnpm lint:fix` で自動修正

## 詰まった点
- `pnpm add` が ERR_PNPM_UNEXPECTED_VIRTUAL_STORE エラーで失敗 → package.json を直接編集して `pnpm install` で対応

## 次のタスクへの申し送り
- `CheckHistoryFilter` はまだどのページにも組み込まれていない。ダッシュボードページ実装タスクで使用する想定
- フィルタ変更時の URL は `?source=web&status=completed&from=2024-01-01&to=2024-01-31` 形式
