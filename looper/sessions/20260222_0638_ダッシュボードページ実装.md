# タスク: dashboard-page

## タスク ID と内容
`dashboard-page`: src/app/(app)/page.tsx を Server Component として更新し、CheckHistoryFilter と CheckHistoryTable を配置。e2e/dashboard.spec.ts を新規作成。

## 作成・変更したファイル
- `src/app/(app)/page.tsx` — SearchParams から filter を構築して loadContentCheckList を呼び出し、ページタイトル・新規チェックボタン・フィルタ UI・テーブルを配置
- `src/backend/contexts/content-check/presentation/loaders/list-content-checks.loader.ts` — 新規作成。composition から UseCase を取得して execute() を呼ぶ loader
- `src/backend/contexts/content-check/presentation/composition/content-check.composition.ts` — createListContentChecksUseCase() ファクトリを追加
- `e2e/dashboard.spec.ts` — 新規作成。ページ表示・フィルタ URL 更新・行クリック遷移・完全 E2E シナリオの 4 describe

## 設計判断
- `page.tsx` は loader 呼び出し + props 渡しのみ（architecture.md 規約に従い）
- SearchParams の source/status は型安全な値のみ filter にセット（不正値は無視）
- CheckHistoryFilter は useSearchParams を使うため `<Suspense>` でラップ（Next.js 要件）
- loader は既存の get-content-check-detail.loader.ts と同パターンで作成

## 詰まった点
- Biome のインポート順（alphabetical）と行長制限でフォーマットエラーが出たが、修正して解決

## 次のタスクへの申し送り
- E2E テストは pnpm verify では実行されない（verify:full が必要）
- ダッシュボードページは動的レンダリング（ƒ）になっている
